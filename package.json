{
  "name": "cgen-url-shortener",
  "version": "1.0.0",
  "description": "CGEN URL Shortener",
  "author": "Tyler Candee",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "bootstrap": "yarn install && lerna bootstrap",
    "build": "build:client && cd server && nest build",
    "build:client": "cd server/client && yarn build",
    "start": "cd server && yarn start"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^1.19.1"
  }
}
